#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int a, b;
	cin >> a >> b;
	if (a != 100 && b != 100 ) {
		cout << "yes";
	}
	else {
		cout << "no";
	}
	return 0;
}